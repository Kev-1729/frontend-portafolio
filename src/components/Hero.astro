<section>
  <div class="hero-wrapper">
    <div class="hero-window">
      <video class="background-video" autoplay loop muted playsinline>
        <source src="/videos/hero-background.mp4" type="video/mp4" />
      </video>
      <div class="video-overlay"></div>
    </div>
    <div class="hero-window">
      <video class="background-video" autoplay loop muted playsinline>
        <source src="/videos/hero-background.mp4" type="video/mp4" />
      </video>
      <div class="video-overlay"></div>
    </div>
    <div class="hero-window">
      <video class="background-video" autoplay loop muted playsinline>
        <source src="/videos/hero-background.mp4" type="video/mp4" />
      </video>
      <div class="video-overlay"></div>
    </div>
  </div>
  <div class="container-hero">
    <div class="overlay-content">
      <div class="text-content-carrera">
        <h1>Ingeniero de Software</h1>
      </div>
      <div class="text-content-descripcion">
        <h3>Creador de soluciones digitales robustas y escalables.</h3>
      </div>
    </div>
    <div class="image-container">
      <img src="/images/profile.png" alt="Tu foto de perfil" />
    </div>
  </div>
</section>

<style>
  .hero-wrapper {
    padding: 3rem;
    height: 100vh;
    display: flex;
    gap: 25px;
    position: relative;
  }

  .hero-window {
    width: 100%;
    height: 75vh; /* Altura definida para el fondo */
    border-radius: 20px;
    position: relative;
    overflow: hidden;
    z-index: 1; /* Se queda en la capa de fondo */
  }

  .background-video,
  .video-overlay {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .video-overlay {
    background: rgba(0, 0, 0, 0.5);
    z-index: 2;
  }

  .container-hero {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
  }

  .overlay-content {
    color: white;
    text-align: left;
  }

  .image-container {
    width: 700px;
    height: auto;
    flex-shrink: 0;
    overflow: hidden;
  }

  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .text-content-carrera h1 {
    font-size: clamp(2rem, 5vw, 2.8rem);
  }

  .text-content-descripcion h3 {
    font-size: clamp(1rem, 2.5vw, 1.1rem);
  }

  /* --- Media Query para Móviles --- */
  @media (max-width: 768px) {
    .container-hero {
      flex-direction: column;
      text-align: center;
      width: 90%;
      margin-top: -120px; /* Ajustamos el margen para móvil */
      padding: 2rem 1.5rem;
    }

    .overlay-content {
      align-items: center;
      text-align: center;
    }

    .image-container {
      width: 140px;
      height: 140px;
      order: -1; /* Pone la imagen arriba */
    }
  }
</style>
